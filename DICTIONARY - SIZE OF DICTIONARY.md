# Exp.No:16  
## DICTIONARY - SIZE OF DICTIONARY

---

### AIM  
To write a Python program to print the size of a dictionary using `getsizeof()` from the `sys` module.

---

### ALGORITHM

1. Begin the program.  
2. Import the `sys` module to use the `getsizeof()` function.  
3. Define the dictionaries with key-value pairs (`dic1`, `dic2`, `dic3`).  
4. Use `sys.getsizeof()` to calculate the memory size of each dictionary.  
5. Print the size of each dictionary in bytes.  
6. Terminate the program.

---

### PROGRAM

```
#Reg.No
#Name
#Add Your Code Here
# Reg.No:
# Name:

# Define dictionaries
dic1 = {1: 'apple', 2: 'banana', 3: 'cherry'}
dic2 = {'a': 100, 'b': 200}
dic3 = {10: 'cat', 20: 'dog', 30: 'elephant', 40: 'fox'}

# Get size of each dictionary
size1 = len(dic1)
size2 = len(dic2)
size3 = len(dic3)

# Print sizes
print(f"Number of items in dic1: {size1}")
print(f"Number of items in dic2: {size2}")
print(f"Number of items in dic3: {size3}")



```

### OUTPUT
![image](https://github.com/user-attachments/assets/66ecae2a-c41a-448d-a992-10ffca06671c)


### RESULT
Thus, the Python program to find and display the size (number of elements) of dictionaries without importing any module was successfully written and executed.
